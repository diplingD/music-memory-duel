// Single source of truth for the 8-key layout — shared by Keyboard.tsx (which key
// maps to which pitch) and PianoRoll.tsx (which lane a pitch renders on).
export const PITCHES = ['C4', 'D4', 'E4', 'F4', 'G4', 'A4', 'B4', 'C5']

export const SHORTCUTS = ['z', 'x', 'c', 'v', 'b', 'n', 'm', ',']
